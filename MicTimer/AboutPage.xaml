<Page x:Class="MicTimer.AboutPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      DataContext="{Binding Main, Source={StaticResource Locator}}"
      >

    <Grid Margin="40,40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" FontSize="60" Foreground="White">MicTimer</TextBlock>
        <TextBlock Grid.Row="1" FontSize="30" Foreground="White" TextWrapping="WrapWholeWords">Thank you for using MicTimer! Please send feature requests to <Hyperlink NavigateUri="mailto:barrett@devfoundries.com">Wm. Barrett Simms (DevFoundries)</Hyperlink>.</TextBlock>
    </Grid>
    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Icon="Back" Label="Back" Click="GoBackButtonClick"></AppBarButton>
        </CommandBar>
    </Page.BottomAppBar>
</Page>
